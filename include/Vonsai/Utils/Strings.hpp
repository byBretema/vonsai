#pragma once

#include <sstream>
#include <string>
#include <vector>

namespace Vonsai::Strings {

std::vector<std::string> split(std::string const &str, std::string const &delimeter);

} // namespace Vonsai::Strings
